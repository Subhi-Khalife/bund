import 'package:flutter/material.dart';

import '../../constant/design/constant.dart';

class SW4 extends StatelessWidget {
  const SW4({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox(width: kswSizeWidth4);
  }
}

class SW8 extends StatelessWidget {
  const SW8({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox(width: kswSizeWidth8);
  }
}

class SW12 extends StatelessWidget {
  const SW12({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox(width: kswSizeWidth12);
  }
}

class SW16 extends StatelessWidget {
  const SW16({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox(width: kswSizeWidth16);
  }
}

class SW24 extends StatelessWidget {
  const SW24({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox(width: kswSizeWidth24);
  }
}

class SW32 extends StatelessWidget {
  const SW32({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox(width: kswSizeWidth32);
  }
}

class SW40 extends StatelessWidget {
  const SW40({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox(width: kswSizeWidth40);
  }
}

class SW48 extends StatelessWidget {
  const SW48({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox(width: kswSizeWidth48);
  }
}

class SW64 extends StatelessWidget {
  const SW64({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox(width: kswSizeWidth64);
  }
}

class SW80 extends StatelessWidget {
  const SW80({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox(width: kswSizeWidth80);
  }
}

class SW96 extends StatelessWidget {
  const SW96({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox(width: kswSizeWidth96);
  }
}
