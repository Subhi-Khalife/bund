import 'package:flutter/material.dart';

import '../../constant/design/constant.dart';

class Sh4 extends StatelessWidget {
  const Sh4({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox(height: kshSizeHeight4);
  }
}

class Sh8 extends StatelessWidget {
  const Sh8({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox(height: kshSizeHeight8);
  }
}

class Sh12 extends StatelessWidget {
  const Sh12({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox(height: kshSizeHeight12);
  }
}

class Sh16 extends StatelessWidget {
  const Sh16({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox(height: kshSizeHeight16);
  }
}

class Sh24 extends StatelessWidget {
  const Sh24({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox(height: kshSizeHeight24);
  }
}

class Sh32 extends StatelessWidget {
  const Sh32({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox(height: kshSizeHeight32);
  }
}

class Sh40 extends StatelessWidget {
  const Sh40({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox(height: kshSizeHeight40);
  }
}

class Sh48 extends StatelessWidget {
  const Sh48({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox(height: kshSizeHeight48);
  }
}

class Sh64 extends StatelessWidget {
  const Sh64({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox(height: kshSizeHeight64);
  }
}

class Sh80 extends StatelessWidget {
  const Sh80({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox(height: kshSizeHeight80);
  }
}

class Sh96 extends StatelessWidget {
  const Sh96({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox(height: kshSizeHeight96);
  }
}
